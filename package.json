{
  "name": "phystack-template-edge-python",
  "version": "1.0.0",
  "description": "PhyStack Edge App Template (Python)",
  "application-type": "edge",
  "container-registry": "omborigridregistry.azurecr.io",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@phystack/ts-schema": "^4.4.74",
    "typescript": "~5.9.3",
    "@types/node": "^24.6.0"
  },
  "scripts": {
    "predev": "bun scripts/init-settings.js",
    "dev": "phy-simulator run . --dev-command 'python src/app.py'",
    "start": "python src/app.py",
    "schema": "mkdir -p build && bunx ts-schema src/schema.ts build/",
    "build": "bun run schema && cp -r src/*.py build/ && cp requirements.txt build/ && touch build/index.html",
    "pub": "bun run build && phy app build create $npm_package_name --dir . --push --publish"
  }
}
